# Keeps the build context small and avoids baking local state into the
# job image. The image builds from this directory, so paths here are
# relative to duui-video-emotion-global-identity/ (this folder).
__pycache__/
*.pyc
.pytest_cache/
tests/
pyproject.toml
*.md
Dockerfile
