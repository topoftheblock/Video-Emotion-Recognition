# Dev/test-only deps -- not needed to run either container, only to run
# the test suites (duui-video-emotion-cas-to-postgres/tests/ and
# webapp/tests/). The app code itself lives entirely inside
# duui-video-emotion-cas-to-postgres/ and webapp/; this is the one
# thing both suites need and neither image ships.
#
#   pip install -r duui-video-emotion-cas-to-postgres/requirements.txt \
#               -r webapp/requirements.txt -r requirements-dev.txt
#   pytest
pytest>=8.0
