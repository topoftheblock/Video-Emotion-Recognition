# Dev/test-only deps -- not needed to run either container, only to run
# the test suites (importer/tests/ and webapp/tests/). The app code
# itself lives entirely inside importer/ and webapp/; this is the one
# thing both suites need and neither image ships.
#
#   pip install -r importer/requirements.txt -r webapp/requirements.txt \
#               -r requirements-dev.txt
#   pytest
pytest>=8.0
