# Keeps the build context small and avoids baking local state into the
# webapp image. The image builds from this directory, so paths here are
# relative to webapp/.
__pycache__/
*.pyc
.pytest_cache/
tests/
pyproject.toml
*.md
build.sh
Dockerfile
