# Legacy documents, kept only as a checklist of topics for the rewrite. Their
# claims are unverified and they are deleted when Phase 7 finishes; reformatting
# them would put churn in a diff that should stay empty.
docs/legacy/

# Vendored font files and their licences.
webapp/src/frontend/fonts/

# Records of past runs, not documents being maintained.
webapp/docs/a11y-baseline/

# Markdown is handled by markdownlint, not by prettier. Prettier's only change to
# these files is rewriting *emphasis* as _emphasis_, a marker swap with no
# readability gain that would churn every document in the repository. markdownlint
# checks markdown style and can be told which marker this project already uses.
*.md
