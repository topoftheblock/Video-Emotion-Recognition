[tool.pytest.ini_options]
# `src` is the source root rather than a package, so putting it on the
# path makes `import backend` resolve exactly as it does in the
# container. This lets the suite run from this directory alone; the
# project root's pyproject.toml runs all three suites together.
pythonpath = ["src"]
testpaths = ["tests"]
