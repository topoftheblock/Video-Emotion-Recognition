# Configuration

Every environment variable, in one place. `.env.example` carries a one-line
gloss for each and links here; the root README lists only what a first run
needs.

> **Stub.** Written in Phase 7. Each variable must be verified against the code
> that reads it, not against `.env.example` or the current README.

## Input and output paths

<!-- DUUI_INPUT_XMI_DIR, DUUI_INPUT_VIDEO_DIR, DUUI_XMI_FILE, DUUI_VIDEO_DIR,
     DUUI_VIDEO_STORE -->

## Database

<!-- DUUI_DB_NAME, DUUI_DB_USER, DUUI_DB_PASSWORD, DUUI_DB_HOST,
     DUUI_DB_HOST_PORT, DUUI_DB_CONNECT_TIMEOUT. Note the placeholder
     defaults and their consequence, and that the connect timeout bounds
     the connection only, never a query already running. -->

## Importer behaviour

<!-- DUUI_ON_EXISTING, DUUI_VIDEO_VIEW, DUUI_TS_* -->

## Identity linking

<!-- DUUI_GLOBAL_PERSON_FACE_DISTANCE_THRESHOLD,
     DUUI_GLOBAL_PERSON_VOICE_DISTANCE_THRESHOLD -->

## Webapp

<!-- DUUI_WEBAPP_HOST_PORT, the Ask panel's LLM settings -->
