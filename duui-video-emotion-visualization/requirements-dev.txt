# Dev/test-only deps -- not needed to run either container, only to run
# the test suites (cas-to-postgres-importer/tests/ and
# webapp/tests/). The app code itself
# lives entirely inside those two directories; this is the one thing
# both suites need and neither image ships.
#
#   pip install -r cas-to-postgres-importer/requirements.txt \
#               -r webapp/requirements.txt \
#               -r requirements-dev.txt
#   pytest
pytest>=8.0

# Required by fastapi.testclient.TestClient, which the viewer's route
# tests use to exercise create_app() without binding a port. Test-only:
# the viewer image serves through uvicorn and never imports it.
httpx>=0.27
